import 'package:flutter/material.dart';

class HomeMenuItemBean {
  String title;
  IconData icon;
  String imageName;
  Widget route;
  Color color;
  HomeMenuItemBean({this.title, this.icon, this.imageName, this.route, this.color});
}